# RAGSTER CONFIGURATION
# Copy to .env and fill in your values

# SAPTIVA API (required)
SAPTIVA_API_KEY=your-api-key
SAPTIVA_API_BASE_URL=https://api.saptiva.com
EMBEDDING_API_URL=https://api.saptiva.com/api/embed
NEXT_PUBLIC_CHAT_API=https://api.saptiva.com/v1/chat/completions

# AUTH (required)
NEXTAUTH_SECRET=generate-a-random-secret-here
# NEXTAUTH_URL=http://localhost:3001  # Default for local, change for production (e.g., https://yourdomain.com)

# MONGODB (Docker local por defecto, cambiar para Cloud Atlas)
MONGODB_URI=mongodb://mongo:27017/ragster
# Para cloud: MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/
# MONGODB_DB_NAME=ragster  # Required for cloud if URI doesn't include DB name

# --- WEAVIATE ---
# Local (Docker): deja comentado
# Cloud: descomenta y configura
# WEAVIATE_CLOUD=true
# WEAVIATE_HOST=tu-cluster.weaviate.cloud
# WEAVIATE_API_KEY=tu-api-key

# --- CLOUDFLARE TURNSTILE (opcional, solo producción) ---
# Crear widget en Cloudflare Dashboard → Turnstile
# Activar "Pre-Clearance" para obtener cf_clearance cookie
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAA...
